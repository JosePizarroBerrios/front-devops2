version: '3.8'

services:
  frontend:
    image: ${DOCKER_USER}/innovatech-frontend:latest
    container_name: frontend_service
    restart: always
    ports:
      - "80:5000"
    env_file:
      - .env
    networks:
      - eval-net

networks:
  eval-net:
    driver: bridge